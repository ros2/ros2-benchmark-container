#!/usr/bin/env python3

# Copyright (c) 2026, iRobot ROS
# All rights reserved.
#
# This source code is licensed under the BSD 3-Clause License found in the
# LICENSE file in the root directory of this source tree.

"""
Generate a Comprehensive PDF Report from Benchmark Results.

This script serves as the final step in the post-processing pipeline. It gathers all
the analyzed data, including plots and statistical summaries from CSV files, and
compiles them into a single, well-structured PDF document.

The report includes:
- An introduction with system information and a list of tested RMWs.
- Detailed sections for different test categories (Topics, Services, Actions).
- Explanations for each specific test case.
- Tables showing statistical results (e.g., from linear regressions).
- The plots generated by other analysis scripts.
"""

import argparse
import csv
import glob
import os
import platform
import re
import sys
from collections import defaultdict
from typing import List, Any, Tuple, Dict, Optional

from reportlab.lib import colors
from reportlab.lib.pagesizes import A4
from reportlab.lib.styles import getSampleStyleSheet
from reportlab.platypus import (
    Image,
    PageBreak,
    Paragraph,
    SimpleDocTemplate,
    Spacer,
    Table,
    TableStyle,
)

# --- ReportLab Style Definitions ---
GLOBAL_STYLES = getSampleStyleSheet()
TITLE_STYLE = "Title"
TEST_SECTION_STYLE = "Heading1"
TEST_SUBSECTION_STYLE = "Heading2"
TEST_DESCRIPTION_STYLE = "Heading3"
NORMAL_TEXT_STYLE = "Normal"


# --- PDF Generation Helper Functions ---
def _add_styled_paragraph(
    elements: List[Any],
    text: str,
    style: str,
    spacer_height: float = 0,
):
    """Helper to add a styled paragraph and an optional spacer."""
    elements.append(Paragraph(text, GLOBAL_STYLES[style]))
    if spacer_height > 0:
        elements.append(Spacer(1, spacer_height))


def add_title(elements: List[Any], text: str):
    """Adds a main title to the list of elements."""
    _add_styled_paragraph(elements, text, TITLE_STYLE, 10)


def add_section_title(elements: List[Any], text: str):
    """Adds a section title to the list of elements."""
    _add_styled_paragraph(elements, text, TEST_SECTION_STYLE)


def add_description_paragraph(elements: List[Any], text: str):
    """Adds a description paragraph to the list of elements."""
    _add_styled_paragraph(elements, text, TEST_DESCRIPTION_STYLE, 10)


def add_subsection_title(elements: List[Any], text: str):
    """Adds a subsection title to the list of elements."""
    _add_styled_paragraph(elements, text, TEST_SUBSECTION_STYLE, 20)


def add_normal_text(elements: List[Any], text: str, spacer_height: float = 20.0):
    """Adds a normal text paragraph to the list of elements, followed by a spacer."""
    _add_styled_paragraph(elements, text, NORMAL_TEXT_STYLE, spacer_height)


def load_plot(plots_directory: str, filename: str, scale: float = 0.45) -> Image:
    """
    Loads a plot image from a file and scales it for the report.

    Args:
        plots_directory: The directory where the plot image is located.
        filename: The filename of the plot image.
        scale: The scaling factor for the image.

    Returns:
        A ReportLab `Image` object containing the imported plot.
    """
    if not filename.endswith(".png"):
        filename += ".png"
    img_path = os.path.join(plots_directory, filename)

    if not os.path.exists(img_path):
        raise FileNotFoundError(f"File not found: {img_path}")

    img = Image(img_path)
    # Scale the image to fit the page
    img._restrictSize(img.imageWidth * scale, img.imageHeight * scale)
    return img


def add_plot(
    elements: List[Any], plots_directory: str, filename: str, scale: float = 0.45
):
    """
    Adds a plot image to the list of elements.

    Args:
        elements: The list of PDF content elements.
        plots_directory: The directory containing the plot images.
        filename: The filename of the plot image.
        scale: The scaling factor for the image.
    """
    try:
        img = load_plot(plots_directory, filename, scale)
        elements.append(img)
        elements.append(Spacer(1, 20))
    except FileNotFoundError as e:
        print(f"Error loading plot {filename}: {e}")


def add_multi_plot(
    elements: List[Any],
    results_dir: str,
    plot_filenames: List[str],
    scale: float = 0.45,
):
    """
    Adds plot images that are divided into multiple files due to exceeding the number of subplots per figure.

    Args:
        elements: The list of content to append to.
        results_dir: The base directory where the plots are located.
        plot_filenames: A list of base filenames for the plots.
        scale: The scaling factor for the images.
    """
    for base_filename in plot_filenames:
        # Create a glob pattern to match base + _optional_suffix
        pattern = os.path.join(results_dir, f"{base_filename}*")
        matching_files = sorted(glob.glob(pattern))
        for filepath in matching_files:
            try:
                filename = os.path.basename(filepath)
                add_plot(elements, results_dir, filename, scale)
            except FileNotFoundError as e:
                print(f"Error: {e}")  # Log and continue


def parse_date_from_dirname(dir: str) -> str:
    """
    Extracts a formatted date string from a directory name.

    Args:
        dir: The directory name containing the date in the format 'DD_MM_YY_HHhMM'.

    Returns:
        A formatted date string (e.g., 'DD-MM-YY - HH:MM') or an empty string if not found.
    """
    match = re.search(r"(\d{2})_(\d{2})_(\d{2})_(\d{2}h\d{2})", dir)
    # Output in format DD-MM-YY HH:SS
    return (
        f"{match.group(1)}-{match.group(2)}-{match.group(3)} - {match.group(4).replace('h', ':')}"
        if match
        else ""
    )


def create_styled_table(data: List[List[Any]]) -> Optional[Table]:
    """
    Creates a ReportLab `Table` object with standard project styling.

    Args:
        data: A list of lists where the first list is the header and the rest are the rows.

    Returns:
        A styled `Table` object, or None if the data contains only a header.
    """
    if len(data) == 1:  # Only headers, no actual data
        return None
    table = Table(data)
    table.setStyle(
        TableStyle(
            [
                ("BACKGROUND", (0, 0), (-1, 0), colors.grey),
                ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                ("ALIGN", (0, 0), (-1, -1), "CENTER"),
                ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                ("BOTTOMPADDING", (0, 0), (-1, 0), 10),
                ("GRID", (0, 0), (-1, -1), 1, colors.black),
                ("FONTSIZE", (0, 0), (-1, -1), 8),
            ]
        )
    )
    return table


def load_and_group_latency_comparison_csv(csv_path: str) -> Dict[str, List[List[Any]]]:
    """
    Loads CSV data from the specified file and group it by the test case column.

    Args:
        csv_path: The path to the CSV file.

    Returns:
       A dictionary with the lists of rows grouped by test case.
    """
    if not os.path.exists(csv_path):
        raise FileNotFoundError(f"CSV file not found: {csv_path}")

    # Read CSV data
    with open(csv_path, newline="", encoding="utf-8") as f:
        reader = csv.reader(f, delimiter=";")
        data = list(reader)

    # Group rows by the 'test_case' column
    grouped_data = defaultdict(list)
    for row in data[1:]:  # Skip the header row
        test_case = row[0]
        grouped_data[test_case].append(row[1:])  # Append the rest of the row

    return grouped_data


def create_tables_from_groups(
    grouped_data: Dict[str, List[List[Any]]],
) -> Dict[str, Table]:
    """
    Creates a styled table for each group of data, grouped by each test case.

    Args:
        grouped_data: A dictionary where the keys are test case values, and the values are lists of rows.

    Returns:
        dict: A dictionary with the Table objects created from the grouped data by each test case.
    """
    tables = {}
    for test_case, rows in grouped_data.items():
        headers = [
            "payload",
            "multi_std_dev",
            "multi_mean",
            "single_std_dev",
            "single_mean",
            "p_value",
        ]
        # Add the header to the rows and pass to create_styled_table function
        tables[test_case] = create_styled_table([headers] + rows)
    return tables


def load_csv_as_table(csv_path: str) -> Table:
    """
    Loads a table from a CSV file in the given directory.

    Args:
        csv_path (str): Path to the CSV file containing the table data.

    Returns:
        A styled `Table` object containing the data from the CSV file.
    """
    if not os.path.exists(csv_path):
        raise FileNotFoundError(f"CSV file not found: {csv_path}")

    with open(csv_path, newline="", encoding="utf-8") as f:
        reader = csv.reader(f, delimiter=";")  # Use ';' as the delimiter
        data = list(reader)
    return create_styled_table(data)


def load_regression_tables(csv_path: str, ros_interface: str) -> Tuple[Table, Table]:
    """
    Loads and parses a CSV file containing linear regression results for different RMW vendors
    under single and multi-process configurations.

    The function reads a CSV file where each row includes the process type, vendor name,
    r-squared value, and slope. It separates the data into two tables: one for single-process
    and one for multi-process results.

    Args:
        csv_path: Path to the CSV file containing the regression results.
        ros_interface: The ROS interface type (e.g., 'pub-sub' or 'cli-srv')

    Returns:
        A tuple containing two `Table` objects: (single_process_table, multi_process_table).
    """
    if not os.path.exists(csv_path):
        raise FileNotFoundError(f"CSV file not found: {csv_path}")

    single_process_data = [["RMW", "r-squared", "slope"]]  # Header
    multi_process_data = [["RMW", "r-squared", "slope"]]  # Header

    with open(csv_path, newline="", encoding="utf-8") as f:
        reader = csv.reader(f, delimiter=";")
        next(reader)  # Skip header row

        for row in reader:
            parts = row[0].split("/")
            # Check if the row has the expected number of parts
            if len(parts) == 3:
                row_interface, process_type, vendor = parts
                if row_interface != ros_interface:
                    continue
            elif len(parts) == 2:
                process_type, vendor = parts
            else:
                print(f"Skipping invalid row: {row}")
                continue
            if "single_process" in process_type:
                single_process_data.append([vendor, row[1], row[2]])
            elif "multi_process" in process_type:
                multi_process_data.append([vendor, row[1], row[2]])
    return create_styled_table(single_process_data), create_styled_table(
        multi_process_data
    )


def get_supported_rmws(csv_path: str) -> List[str]:
    """
    Retrieves a list of supported ROS Middleware (RMW) configurations from the metrics csv file.

    Args:
        csv_path: The path to the directory containing the csv file.

    Returns:
        A sorted list of unique RMW names.
    """
    rmw_list = []

    if not os.path.exists(csv_path):
        raise FileNotFoundError(f"CSV file not found: {csv_path}")

    with open(csv_path, newline="", encoding="utf-8") as f:
        reader = csv.reader(f, delimiter=";")
        next(reader)  # Skip header row

        for row in reader:
            # Get the 'RMW' column value and split the path to get the RMW name
            parts = row[0].split("/")
            rmw_name = parts[2].split("_")[0]
            if rmw_name not in rmw_list:
                rmw_list.append(rmw_name)
    return sorted(rmw_list)


def generate_report(
    results_dir: str, output_filename: str, skip_long_tests: bool
) -> None:

    def _load_or_exit(loader_func, *args, **kwargs):
        """Helper to load data using a loader function or exit on FileNotFoundError."""
        try:
            return loader_func(*args, **kwargs)
        except FileNotFoundError as e:
            print(
                f"Error: {e}. Process cannot continue. Ensure all expected files are present."
            )
            sys.exit(1)

    # Create a landscape PDF with custom margins
    doc = SimpleDocTemplate(
        output_filename,
        pagesize=A4,
        leftMargin=50,  # Set left margin
        rightMargin=50,  # Set right margin
        topMargin=30,  # Set top margin
        bottomMargin=30,  # Set bottom margin
    )
    elements: List[Any] = []  # List to store content

    date = parse_date_from_dirname(results_dir)
    supported_rmws = _load_or_exit(
        get_supported_rmws, os.path.join(results_dir, "report_metrics.csv")
    )

    single_latency_table, multi_latency_table = _load_or_exit(
        load_regression_tables,
        os.path.join(results_dir, "report_latency.csv"),
        "pub-sub",
    )
    single_cpu_table, multi_cpu_table = _load_or_exit(
        load_regression_tables,
        os.path.join(results_dir, "report_metrics.csv"),
        "pub-sub",
    )

    # --- Report Content Definitions ---
    REGRESSION_ANALYSIS_TEXT = (
        "The following table shows the results of a linear regression analysis for the different RMW (ROS Middleware) configurations. The columns are:<br/>"
        "<b>• r-squared:</b> The coefficient of determination, indicating how well the linear model fits the data. "
        "A value close to <b>1</b> suggests a strong correlation, while a value near <b>0</b> indicates a weak or no correlation.<br/>"
        "<b>• Slope:</b> The rate of change in the dependent variable with respect to the independent variable. "
        "A slope close to <b>0</b> suggests minimal variation in the observed trend."
        "<br/><br/>"
    )

    # Title
    add_title(elements, f"ROS 2 Benchmark Report - {date}")
    add_normal_text(
        elements,
        "This document presents a comprehensive benchmark report evaluating the performance characteristics of a ROS 2 application "
        "across various ROS Middleware (RMW) configurations to analyze different metrics like latency, CPU and RAM usage.<br/><br/>"
        "The report details a series of test cases, including:<br/>"
        "• Topic-based communication under varying message sizes, single and multi-process setups, and long-duration runs.<br/>"
        "• Service and Action communication, testing the influence of client numbers and long-duration runs.<br/>"
        "• Idle system resource usage and RAM usage scalability with an increasing number of processes.<br/><br/>"
        "Each test uses a different configuration file that defines the RMWs to be tested, their respective communication settings "
        "(e.g., ipc_on, ipc_off, loaned) and the topologies involved.",
    )
    add_normal_text(
        elements,
        f"The following RMWs were tested: <b>{', '.join(supported_rmws)}</b>.<br/>",
    )

    add_normal_text(
        elements,
        "System Information:<br/>"
        f"• <b>ROS Version:</b> {os.getenv('ROS_DISTRO', 'unknown')}<br/>"
        f"• <b>System Executor:</b> {os.getenv('SYSTEM_EXECUTOR', 'unknown')}<br/>"
        f"• <b>Architecture:</b> {platform.machine()}<br/>",
    )

    # Subtitle
    add_section_title(elements, "1. Topic Tests")

    # Test 1.a
    add_description_paragraph(
        elements, "1.a. Payload Size Impact on Single-Process Performance"
    )

    add_normal_text(
        elements,
        "This test evaluates how CPU and latency are affected by payload size in single-process applications, using various RMW configurations"
        "It uses messages with fixed and dynamic array sizes of 10b, 100kb, 1MB, and 4MB, while keeping the publishing frequency constant at 200 Hz.<br/>"
        "The test is configured using the <i>single_process_pub_sub.conf</i> file. The topology involves a single publisher node (pub_node_sp) "
        "and a single subscriber node (sub_node_sp) communicating over a single topic.",
    )

    add_normal_text(
        elements,
        "Ideally, the tendency approximation curve in the following test cases should remain constant. "
        "Mathematically, this implies an r-squared value of 0 and a slope of 0 for the linear regression. "
        "However, to account for natural variance, we will consider values within an acceptable threshold as valid.",
    )

    # Latency subtitle
    add_subsection_title(elements, "Latency Analysis")

    # Table description
    add_normal_text(
        elements,
        REGRESSION_ANALYSIS_TEXT,
    )

    # Latency usage table
    elements.append(single_latency_table)
    elements.append(Spacer(1, 50))

    # Note on the linear regression
    add_normal_text(
        elements,
        "<b>Note:</b>: The linear regression trend in the following figures may appear curved in the graph due to the logarithmic scale applied to the x-axis. "
        "This transformation compresses larger values, causing a visually nonlinear effect, "
        "even though the regression itself remains a straight-line approximation in linear space.",
    )

    # Latency plots
    latency_plots_filenames = [
        "latency_pub-sub_single_process_PubDur",
        "latency_pub-sub_single_process_SubLat",
    ]
    add_multi_plot(elements, results_dir, latency_plots_filenames)

    elements.append(PageBreak())

    # CPU Usage subtitle
    add_subsection_title(elements, "CPU Usage Analysis")

    add_normal_text(
        elements,
        REGRESSION_ANALYSIS_TEXT,
    )
    elements.append(single_cpu_table)

    add_multi_plot(elements, results_dir, ["cpu_usage_pub-sub_single_process"])

    elements.append(Spacer(1, 20))

    # Link to the Boxplot documentation
    add_normal_text(
        elements,
        "For information on the graphic visit the "
        '<a href="https://matplotlib.org/stable/api/_as_gen/matplotlib.pyplot.boxplot.html" color="blue">Boxplot documentation</a>',
    )

    # Page brake
    elements.append(PageBreak())

    # Test 1.b
    add_description_paragraph(
        elements, "1.b. Payload Size Impact on Multi-Process Performance"
    )
    add_normal_text(
        elements,
        "This test evaluates how CPU and latency of a ROS 2 application are affected by the size of messages "
        "for multi-process applications on the same host, specifically in the case of POD (Plain Old Data) types. "
        "The test utilizes messages with fixed payload sizes of 10b, 100kb, 1MB, and 4MB.<br/><br/>"
        "The test is configured using the <i>pub-sub_multi_process.conf</i> file. This configuration defines paired topologies for each of the payloads, "
        "pub_200hz_PAYLOAD and sub_PAYLOAD,  which are varied to change the payload size (10b, 100kb, 1MB, 4MB).<br/><br/>"
        "Each topology pub_200hz_PAYLOAD includes a publisher node (pub_node) publishing to a single topic with a stamped10b message type at 200 Hz, "
        "while the sub_PAYLOAD topology includes a subscriber node (sub_node) listening to the same topic.",
    )

    # Latency subsection
    add_subsection_title(elements, "Latency")

    elements.append(multi_latency_table)
    latency_plots_filenames = [
        "latency_pub-sub_multi_process_scalability_PubDur",
        "latency_pub-sub_multi_process_scalability_SubLat",
    ]

    add_multi_plot(elements, results_dir, latency_plots_filenames)

    # CPU usage subsection
    add_subsection_title(elements, "CPU Usage")
    elements.append(multi_cpu_table)
    add_multi_plot(elements, results_dir, ["cpu_usage_pub-sub_multi_process"])
    elements.append(PageBreak())

    # Test 1.c
    add_description_paragraph(
        elements,
        "1.c. Comparison of Multi-Process vs. Single-Process Performance",
    )

    add_normal_text(
        elements,
        "This test compares the CPU and latency of a multi-process single-host against an equivalent single-process application when using POD message types.<br/><br/>"
        "This case follows a statistical approach using the p-value to determine the strength of evidence against the null hypothesis. "
        "In the context of this test, the null hypothesis states that a multi-process application using RMW "
        "has equal or higher CPU usage and latency compared to an equivalent single-process application.<br/><br/>"
        "If the p-value is low (typically ≤ 0.05), there is enough evidence to reject the null hypothesis and conclude "
        "that multi-process latency is significantly lower.<br/><br/>"
        "On the other hand, if the p-value is high (> 0.05), you fail to reject it, meaning there isn't enough statistical "
        "evidence to claim that multi-process performs better than single-process.",
    )

    # Latency subsection
    add_subsection_title(elements, "Latency")

    latency_comparison_csv = os.path.join(results_dir, "latency_comparison.csv")
    # Load and group data from the CSV
    grouped_latency_data = _load_or_exit(
        load_and_group_latency_comparison_csv, latency_comparison_csv
    )

    # Create tables for each test_case group
    tables = create_tables_from_groups(grouped_latency_data)

    for test_case, table in tables.items():
        centered_text_style = GLOBAL_STYLES[TEST_SUBSECTION_STYLE]
        centered_text_style.alignment = 1
        elements.append(Paragraph(test_case, centered_text_style))
        elements.append(table)
        elements.append(Spacer(1, 20))

    # CPU Usage subsection
    add_subsection_title(elements, "CPU Usage")

    add_normal_text(
        elements,
        "<b>Note</b>: For multi-process CPU usage, the reported metrics represent the combined CPU consumption "
        "of both the publisher and subscriber processes. This ensures a fair comparison against the single-process case, "
        "where all operations run within a single instance.",
    )

    cpu_usage_comparison_csv = os.path.join(results_dir, "cpu_usage_comparison.csv")
    # Load and group data from the CSV
    grouped_cpu_usage_data = _load_or_exit(
        load_and_group_latency_comparison_csv, cpu_usage_comparison_csv
    )

    # Create tables for each test_case group
    tables = create_tables_from_groups(grouped_cpu_usage_data)

    for test_case, table in tables.items():
        centered_text_style = GLOBAL_STYLES[TEST_SUBSECTION_STYLE]
        centered_text_style.alignment = 1
        elements.append(Paragraph(test_case, centered_text_style))
        elements.append(table)
        elements.append(Spacer(1, 20))

    # Test 1.d
    elements.append(PageBreak())
    add_description_paragraph(
        elements,
        "1.d. Combined Communication Transports.",
    )
    add_normal_text(
        elements,
        "This test evaluates the performance of a ROS 2 application when combining multiple communication transports for a topic. "
        "To evaluate this hypothesis, we compare two different application configurations using a z-test to analyze Latency and CPU usage. <br/><br/>"
        "The first setup involves a single publisher sending messages on a single topic to three subscribers: one in the same process, "
        "one in a different process on the same host, and one on a remote host. This configuration is defined by the <i>mix_process_pub_sub.conf</i> file "
        "and a topology where a pub_node_sp publishes to a topic and a sub_node_sp subscribes to the same topic. <br/><br/>"
        "The second setup uses the <i>mix_process_pub_sub_multiple_topics.conf</i> file, where separate publishers and topics are used for each of the three subscribers "
        "(same process, different process on same host, remote).<br/><br/>"
        "The z-test used for this analysis is a statistical test used to determine if there is a significant difference between the means of two groups.<br/><br/>"
        "A low p-value (typically <0.05) indicates strong evidence that the difference suggests potential performance degradation. "
        "On the other hand, a high p-value supports the hypothesis by indicating that any observed differences in performance are not statistically significant. <br/><br/>"
        "The following tables show the results of the z-test for both latency and CPU usage.",
    )

    add_subsection_title(elements, "Latency")
    latency_table = _load_or_exit(
        load_csv_as_table,
        os.path.join(results_dir, "latency_comparison_mix_process.csv"),
    )
    elements.append(latency_table)

    add_subsection_title(elements, "CPU Usage")
    cpu_table = _load_or_exit(
        load_csv_as_table,
        os.path.join(results_dir, "cpu_usage_comparison_mix_process.csv"),
    )
    elements.append(cpu_table)

    # Test 1.e
    elements.append(PageBreak())
    add_description_paragraph(elements, "1.e. Long-Running Performance Stability")

    if skip_long_tests is False:
        add_normal_text(
            elements,
            "This test evaluates the stability of CPU, latency, and RAM usage over an extended period (greater than 10 minutes). <br/><br/>"
            "The test is run using the <i>single_process_pub_sub_long.conf</i> configuration file and uses the Sierra Nevada topology, "
            "which represents a light 10-node system with multiple topics.<br/><br/>"
            "The metrics are sampled every 10 seconds. The first and last 30 seconds of the test are excluded to remove "
            "initialization and shutdown effects. Each point represents the mean value within a 10-second bin, "
            "helping to smooth out short-term fluctuations. <br/><br/>"
            "A stable system should show relatively flat trends, with variations staying within the 10% coefficient of "
            "variation threshold. Any significant deviations or increasing trends may indicate performance degradation, "
            "resource leaks, or instability in the RMW implementation.",
        )
        # Latency subsection
        add_section_title(elements, "Latency")
        latency_plots_pub_sub_long_filenames = [
            "latency_pub-sub_PubDur_long_test",
            "latency_pub-sub_SubLat_long_test",
        ]

        add_multi_plot(elements, results_dir, latency_plots_pub_sub_long_filenames)

        add_section_title(elements, "CPU Usage")
        add_plot(elements, results_dir, "cpu_perc_pub-sub_long_test")
    else:
        add_normal_text(
            elements,
            "<b>Note:</b> Long-duration test results were skipped. To include them, run the benchmark without the "
            "--skip-long-tests option or set the test duration to more than 60 seconds.",
        )

    # Test 1.f
    elements.append(PageBreak())
    add_description_paragraph(elements, "1.f. CPU Overhead with Remote Subscription")
    add_normal_text(
        elements,
        "This test aims to evaluate the CPU overhead between publishing 1MBps of data across 50 topics "
        "(with a variety of different message types and publish frequencies), and when a remote host subscribes to all these topics.<br/><br/>"
        "The remote connection behavior involves a different script to be executed on the remote host which launches a docker that subscribes to all these topics.<br/><br/>"
        "The configuration file used for this test is <i>single_process_pub_sub_multiple_topics.conf</i>, and uses the topology_50_topics_2000Hz "
        "which defines a single publisher node with 50 publishers, each sending stamped1mb messages on different topics at various frequencies. "
        "And a corresponding subscriber node is configured to subscribe to all 50 topics.",
    )

    add_section_title(elements, "CPU Usage")
    add_plot(elements, results_dir, "cpu_usage_over_time_multiple_topics_local_test")

    try:
        add_plot(
            elements, results_dir, "cpu_usage_over_time_multiple_topics_remote_test"
        )
    except FileNotFoundError as e:
        print(
            f'Could not generate report for remote tests. Was this run with "--skip-remote-tests"? {e}'
        )

    # Services section
    elements.append(PageBreak())
    add_section_title(elements, "2. Services Tests")

    # Test 2.a
    add_description_paragraph(
        elements,
        "2.a CPU Usage and Service Client Count.",
    )
    add_normal_text(
        elements,
        "This test evaluates the CPU usage under varying numbers of connected service clients. "
        "A single service server is deployed alongside a varying number of clients (1, 2, 5, 10, and 20). "
        "In each test scenario, only a subset of the clients actively sends service requests, while the others remain idle by using a really high periods. "
        "The goal is to verify that the CPU usage is only affected by the number of active clients, and not by the total number of clients connected.<br/><br/>"
        "The single-process test uses all components within the same process, while the multi-process test runs each client and server as separate processes. "
        "The tests are configured using the <i>single_process_cli_srv_multiple_clients.conf</i> and <i>multi_process_cli_srv_multiple_clients.conf</i> files respectively.<br/><br/>"
        "An example topology used for this test is 5_cli_srv_100kb, where a server_node provides a stamped100kb service, and a multi_node includes 4 clients "
        "configured to send requests at a very high period (600000 ms), effectively making them idle.<br/><br/>"
        "The tendency approximation curve should remain constant. This implies an r-squared value of 0 and a slope of 0 for the linear regression. "
        "However, to account for natural variance, we will consider values within an acceptable threshold as valid.",
    )
    single_process_cpu_table, multi_process_cpu_table = load_regression_tables(
        os.path.join(results_dir, "report_metrics.csv"), "cli-srv"
    )

    add_subsection_title(elements, "Single-Process")
    elements.append(single_process_cpu_table)
    add_multi_plot(elements, results_dir, ["cpu_usage_cli-srv_single_process"])

    add_subsection_title(elements, "Multi-Process")
    elements.append(multi_process_cpu_table)
    add_multi_plot(elements, results_dir, ["cpu_usage_cli-srv_multi_process"])
    elements.append(PageBreak())

    # Test 2.b
    add_description_paragraph(
        elements,
        "2.b Long-Running Service Performance Stability.",
    )
    add_normal_text(
        elements,
        "This test assesses whether the CPU, latency, and RAM of a ROS 2 application remain constant "
        "over the duration of a long-running test (greater than 10 minutes).<br/><br/>"
        "The metrics are sampled every 10 seconds. And the first and last 30 seconds of the test are discarded to avoid "
        "initialization and shutdown artifacts.<br/><br/>"
        "The test is configured using the single_process_cli_srv_long.conf file, which uses a graph topology cli_srv_architecture"
        "that includes multiple service servers and multiple service clients.",
    )

    # Latency subsection
    add_subsection_title(elements, "Latency")
    latency_plots_cli_srv_long_filenames = [
        "latency_cli-srv_CliLat_long_test",
        "latency_cli-srv_SrvLat_long_test",
    ]
    add_multi_plot(elements, results_dir, latency_plots_cli_srv_long_filenames)

    # CPU Usage subsection
    add_subsection_title(elements, "CPU Usage")
    add_plot(elements, results_dir, "cpu_perc_cli-srv_long_test")

    # RAM Usage subsection
    add_subsection_title(elements, "RAM Usage")
    add_plot(elements, results_dir, "rss_KB_cli-srv_long_test")

    # Actions section
    elements.append(PageBreak())
    add_section_title(elements, "3. Actions Tests")

    add_description_paragraph(
        elements,
        "3.a CPU Usage and Action Client Count.",
    )
    add_normal_text(
        elements,
        "This test evaluates the CPU usage under varying numbers of connected action clients. "
        "A single service server is deployed alongside a varying number of clients (1, 2, 5, 10, and 20). "
        "In each test scenario, only a subset of the action clients actively sends service requests, while the others remain idle by using a really high periods. "
        "The goal is to verify that the CPU usage is only affected by the number of active clients, and not by the total number of clients connected.<br/><br/>"
        "The test is configured using the <i>single_process_actions_multiple_clients.conf</i> file. "
        "An example topology used for this test is 5_cli_srv_100kb, where a server_node provides a stamped100kb service, and a multi_node includes 4 clients "
        "configured to send requests at a very high period (600000 ms), effectively making them idle.<br/><br/>"
        "The tendency approximation curve should remain constant. This implies an r-squared value of 0 and a slope of 0 for the linear regression. "
        "However, to account for natural variance, we will consider values within an acceptable threshold as valid.",
    )
    single_cpu_table, _ = load_regression_tables(
        os.path.join(results_dir, "report_metrics.csv"), "actions"
    )
    elements.append(single_cpu_table)
    add_multi_plot(elements, results_dir, ["cpu_usage_actions_multi_process"])
    elements.append(PageBreak())

    # Test 3.b
    add_description_paragraph(
        elements,
        "3.b Long-Running Action Performance Stability.",
    )
    add_normal_text(
        elements,
        "This test assesses whether the CPU, latency, and RAM of a ROS 2 application remain constant "
        "over the duration of a long-running test (greater than 10 minutes).<br/><br/>"
        "The metrics are sampled every 10 seconds. And the first and last 30 seconds of the test are discarded to avoid "
        "initialization and shutdown artifacts.<br/><br/>"
        "The test is configured using the single_process_actions_long.conf file, which uses a graph topology cli_srv_architecture"
        "that includes multiple action servers and multiple action clients.",
    )

    # Latency subsection
    add_subsection_title(elements, "Latency")
    latency_plots_actions_long_filenames = [
        "latency_actions_ActionCliLat_long_test",
        "latency_actions_ActionSrvLat_long_test",
    ]
    add_multi_plot(elements, results_dir, latency_plots_actions_long_filenames)

    # CPU Usage subsection
    add_subsection_title(elements, "CPU Usage")
    add_plot(elements, results_dir, "cpu_perc_actions_long_test")

    # RAM Usage subsection
    add_subsection_title(elements, "RAM Usage")
    add_plot(elements, results_dir, "rss_KB_actions_long_test")

    elements.append(PageBreak())

    # Test 4.a
    add_description_paragraph(
        elements,
        "4.a. CPU Usage in Idle state",
    )
    add_normal_text(
        elements,
        "This test evaluates the idle CPU usage when no messages are being actively published or received, "
        "and no user-space periodic tasks are running. To simulate this, publishers and subscribers are created "
        "with an extremely long publishing period, set to 1 hour, ensuring that no actual messages are transmitted during the test duration. <br/><br/>"
        "The test is configured using the <i>single_process_pub_sub_idle.conf</i> file, which sets the pub_sub_idle topology. "
        "This topology defines an idle_pub_node with a publisher sending stamped100kb messages at an extremely low frequency "
        "(period_ms: 3600000, i.e. one publish per hour), and an idle_sub_node subscribing to the same idle_topic. <br/><br/>"
        "The test runs for at least 1 minute to avoid measurement bias caused by CPU spikes typically observed during node startup or shutdown.",
    )
    add_plot(elements, results_dir, "cpu_usage_over_time_idle_local_test")

    # Test 4.b
    add_description_paragraph(
        elements,
        "4.b RAM usage of a ROS 2 application should scale linearly with the number of processes.",
    )
    add_normal_text(
        elements,
        "The test compares RAM usage across configurations with 1, 2, 5, and 10 processes, where each process runs a single node. "
        "It expects the RAM usage to scale linearly with the number of processes.<br/><br/>"
        "The test is configured using the <i>multi_process_scalability_pub_sub.conf</i> file. It utilizes topology_multi_1_topics_1000hz "
        "for 1, 2, 5, and 10 topics, respectively, to simulate varying numbers of processes.<br/><br/>"
        "Linear regression is used to evaluate whether the observed RAM usage aligns with this expected linear scaling behavior.",
    )
    add_plot(elements, results_dir, "ram_scalability")

    # Build PDF with all elements
    doc.build(elements)


def main() -> None:
    # Create the argument parser
    parser = argparse.ArgumentParser(
        description="""
This script generates a PDF report ('report.pdf') from the post-process data generated by the benchmarks.

The report is saved in the current working directory.

Usage:
    python generate_report.py <results_directory>  

Arguments:
    results_directory (str): Path where the plots files are located.       
    --output, -o (str, optional): Directory where the report will be saved. Defaults to the current working directory.                      
    --skip-long-tests (flag, optional): If present, skips processing of long-duration test results.
"""
    )

    # Add a required positional argument for the results directory path
    parser.add_argument(
        "results_directory",
        type=str,
        help="The path where the results files will be searched and the output will be saved.",
    )

    # Add an optional argument for the output directory (default is current working directory)
    parser.add_argument(
        "--output",
        "-o",
        type=str,
        default=os.getcwd(),
        help="Directory where the report will be saved. Defaults to the current working directory.",
    )

    # Optional flag to skip long-duration tests
    parser.add_argument(
        "--skip-long-tests",
        action="store_true",
        help="If set, skips processing of long-duration test results.",
    )

    # Parse the arguments
    args = parser.parse_args()

    if not os.path.isdir(args.results_directory):
        print(f"Error: '{args.results_directory}' is not a valid directory.")
        sys.exit(1)

    parsed_results_directory = os.path.join(args.results_directory, "parsed_results")
    os.makedirs(parsed_results_directory, exist_ok=True)

    # Generate report
    results_date = os.path.basename(os.path.normpath(args.results_directory)).replace(
        "results_", ""
    )
    report_path = os.path.join(args.output, f"report_{results_date}.pdf")
    print(f"Generating report {report_path}")
    generate_report(parsed_results_directory, report_path, args.skip_long_tests)


if __name__ == "__main__":
    main()
